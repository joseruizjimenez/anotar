.page-header
  %h1
    Search results
    %small= "(Found #{@hashtags.size} hashtags)"
-# Hashtags list
%ul.unstyled.nav.nav-tabs.nav-stacked.hashtag-list
  -unless @hashtags.blank?
    - @hashtags.each do |tag|
      %li= raw "<a href='/hashtags/#{tag.name}'># #{tag.name}</a>"
.span5
%a.btn.btn-info.btn-large{ :href => notes_path }
  %i.icon-home.icon-white
  Return